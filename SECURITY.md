# Security

Report security issues privately to the repository owner before public disclosure.

TokenGlance does not use private provider APIs, credential extraction, browser scraping, proxy interception, packet capture, privileged helpers, or administrator permissions.

The app is not Developer ID signed and is not notarized. Releases include SHA-256 checksums so users can verify downloaded archives.

